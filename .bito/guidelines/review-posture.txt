Review this pull request like the tech lead of the contentful-sdk-core project.

This is a shared library consumed by contentful.js and contentful-management.js. Changes here ripple to all downstream SDKs.

- Prefer a few high-signal findings to a long list of minor or style-only comments.
- Prefer behavior, contract, runtime, and compatibility issues over process-only suggestions.
- Pay special attention to:
  - Breaking changes to the public API surface (exported types, function signatures)
  - Interceptor ordering changes (auth → throttle → retry is load-bearing)
  - Rate limit / retry logic correctness
  - Axios compatibility (this library wraps and extends axios)
- Keep feedback actionable: explain why it matters, how it would surface in practice, and the clearest next step.
- If a concern is only a risk or assumption rather than a confirmed bug, say that clearly and explain what evidence would confirm it.
- If you find no issues, say so explicitly and call out any residual uncertainty that still deserves human attention.
